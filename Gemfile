source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'
gem 'connection_pool', '2.5.0'

# Stdlib libraries extracted from Ruby defaults — required on Ruby 3.4+/4.0
# for local Jekyll 3.x builds (no effect on GitHub Pages' own build environment).
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'logger'
gem 'ostruct'
gem 'mutex_m'
